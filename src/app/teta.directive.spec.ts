import { TetaDirective } from './teta.directive';

describe('TetaDirective', () => {
  it('should create an instance', () => {
    const directive = new TetaDirective();
    expect(directive).toBeTruthy();
  });
});
